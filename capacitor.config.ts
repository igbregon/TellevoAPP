import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.TellevoApp.app',
  appName: 'TellevoApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
